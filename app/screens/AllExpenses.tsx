import { StyleSheet, View } from "react-native";
import ExpensesOutput from "../components/ExpenseOutput";

const AllExpenses = () => {
  return (
    <View>
      <ExpensesOutput />
    </View>
  );
};

export default AllExpenses;

const styles = StyleSheet.create({});
